<nav class="navbar navbar-expand-sm bg-dark navbar-dark">
  <!-- Brand -->
  <a class="navbar-brand" href="<%= admin_lab_forums_path %>"><%= t('navbar.hans_web') %></a>

  <!-- Links -->
  <ul class="navbar-nav">
    <li class="nav-item"><a class="nav-link" href="<%= resume_path %>"><%= t('navbar.profile') %></a></li>

    <!-- Dropdown -->
    <li class="nav-item dropdown">
      <a class="nav-link dropdown-toggle" href="#" id="navbardrop" data-toggle="dropdown">
        CRUD
      </a>
      <div class="dropdown-menu">
        <a class="dropdown-item" href="<%= admin_lab_forums_path %>"><%= t('navbar.lab_forum') %></a>
        <a class="dropdown-item" href="<%= assignment_todos_path %>"><%= t('navbar.todo') %></a>
        <a class="dropdown-item" href="<%= rooms_path %>"><%= t('navbar.chatroom') %></a>
      </div>
    </li>

    <!-- Dropdown -->
    <li class="nav-item dropdown">
      <a class="nav-link dropdown-toggle" href="#" id="navbardrop" data-toggle="dropdown">
        API
      </a>
      <div class="dropdown-menu">
        <a class="dropdown-item" href="<%= api_v1_ideas_path %>"><%= t('navbar.ideaboard') %></a>
      </div>
    </li>

    <!-- Dropdown -->
    <li class="nav-item dropdown">
      <a class="nav-link dropdown-toggle" href="#" id="navbardrop" data-toggle="dropdown">
        <%= t('navbar.pages') %>
      </a>
      <div class="dropdown-menu">
        <a class="dropdown-item" href="<%= page_path('lotto') %>"><%= t('navbar.lotto') %></a>
        <a class="dropdown-item" href="<%= page_path('squared') %>"><%= t('navbar.squared_up') %></a>
        <a class="dropdown-item" href="<%= page_path('todo') %>"><%= t('navbar.todo_daily') %></a>
        <a class="dropdown-item" href="<%#= bmi_path %>"><%= t('navbar.bmi_cal') %></a>
        <a class="dropdown-item" href="<%#= name_path %>"><%= t('navbar.name_form') %></a>
      </div>
    </li>

    <!-- Dropdown -->
    <li class="nav-item dropdown">
      <a class="nav-link dropdown-toggle" href="#" id="navbardrop" data-toggle="dropdown">
        <%= t('navbar.template') %>
      </a>
      <div class="dropdown-menu">
        <a class="dropdown-item" href="<%= card_path %>"><%= t('navbar.card') %></a>
        <a class="dropdown-item" href="<%= testimonial_path %>"><%= t('navbar.testimonial') %></a>
        <a class="dropdown-item" href="<%= page_path('blog') %>"><%= t('navbar.blog') %></a>
      </div>
    </li>
  </ul>

  <ul class="navbar-nav ml-auto">
    <%= content_tag :li do %>
      <li class="nav-item"><%= link_to 'English', { locale: :en }, class: "nav-link" %></li>
    <% end unless current_page?(locale: :en) %>
    <%= content_tag :li do %>
      <li class="nav-item"><%= link_to '繁體中文', { locale: :"zh-TW" }, class: "nav-link" %></li>
    <% end unless current_page?(locale: :"zh-TW") %>
  </ul>

  <ul class="navbar-nav text-right">
    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
      <% if current_user %>
        <img class="avatar" src="<%= current_user.gravatar_url %>">
        <%= current_user.username %>
      <% else %>
        <%= t('navbar.account') %>
      <% end %>
    </a>
    <div class="dropdown-menu  dropdown-menu-right">
      <% if current_user %>
        <%= link_to t('navbar.logout'), destroy_user_session_path, method: :delete, class: "dropdown-item" %>
      <% else %>
        <a class="dropdown-item" href="<%= new_user_registration_path %>"><span class="fa fa-user"></span><%= t('navbar.signup') %></a>
        <a class="dropdown-item" href="<%= new_user_session_path %>"><span class="fa fa-sign-in"></span><%= t('navbar.login') %></a>
      <% end %>
    </div>
  </ul>
</nav>

